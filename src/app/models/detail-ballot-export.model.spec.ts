import { DetailBallotExport } from './detail-ballot-export.model';

describe('DetailBallotExport', () => {
  it('should create an instance', () => {
    expect(new DetailBallotExport()).toBeTruthy();
  });
});
