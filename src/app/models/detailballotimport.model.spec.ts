import { Detailballotimport } from './detailballotimport.model';

describe('Ballotimport', () => {
  it('should create an instance', () => {
    expect(new Detailballotimport()).toBeTruthy();
  });
});
