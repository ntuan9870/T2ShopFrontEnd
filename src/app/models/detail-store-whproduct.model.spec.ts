import { DetailStoreWHProduct } from './detail-store-whproduct.model';

describe('DetailStoreWHProduct', () => {
  it('should create an instance', () => {
    expect(new DetailStoreWHProduct()).toBeTruthy();
  });
});
